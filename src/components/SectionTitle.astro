---
interface Props {
  title: string;
  As?: string;
}

const { title, As = "h3" } = Astro.props;
---

<As
  id={title.toLowerCase()}
  class="text-lg font-bold text-l/primary dark:text-d/primary">{title}</As
>
